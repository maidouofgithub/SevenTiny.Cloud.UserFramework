# 登陆

## Web

调用登陆接口 [email,phone,password]
{
	校验信息，并获取userid
	生成并返回token
}=>[userid,...]

->

校验本地（cookie，localstorage）是否有SecretKey，如果没有则调用获取SecretKey的接口 [userId] 
{
	ip和userId缓存防撞库，但是如果重新登陆的话则清除缓存
}将返回值存在本地
